# 更新日志

本文档记录 Codex Switcher 面向用户的重要变化。开发中的内容维护在“未发布”章节，发布时由脚本转换为对应版本和日期。

英文版本维护在 [CHANGELOG.en.md](./CHANGELOG.en.md)，发布时两份日志必须保持版本同步。

## [未发布]

### 新增

- 列表模式新增手动重置额度展示，在订阅到期前显示可用次数和最近到期日期；悬停或键盘聚焦可查看每项额度详情。

### 优化

- 重置额度改为账户级共享缓存并合并并发请求，切换列表与卡片布局时不再重复请求；单账户刷新和全部刷新会同步更新重置额度。
- 优化列表账户行布局，重新平衡账户、用量、重置额度、订阅到期与操作区域的间距，并统一重置额度与订阅信息的悬停样式。

## [0.107.4] - 2026-08-21

### 优化

- 自动预热模型更新为 `gpt-5.6-luna`，并将最小预热提示词调整为 `Thanks`，同步上游预热策略。

## [0.107.3] - 2026-08-10

### 修复

- 修复标题栏将 UTC 误标为 OpenAI 时间的问题；现在显示 OpenAI 总部所在地旧金山时间，并自动适配夏令时与冬令时。
- 恢复 Windows 托盘应用的关闭行为：点击主窗口叉号或任务栏“关闭窗口”只隐藏窗口，只有托盘菜单中的“退出 Codex Switcher”会彻底退出应用。

## [0.107.2] - 2026-08-10

### 修复

- 修复 Windows 任务栏组件与 TrafficMonitor 等相邻组件发生重叠时，账号名称仍按完整组件宽度判断而不触发中间省略的问题。
- 修复 Windows 正式版从标题栏或任务栏执行“关闭窗口”时仅隐藏到托盘、无法真正退出的问题。

## [0.107.1] - 2026-08-01

### 新增

- ChatGPT 账户详情新增“录入停用邮件”入口，无需启动重新授权即可粘贴 OpenAI 停用通知；应用会识别并核对账户邮箱，将邮件中的停用时间作为结构化诊断信息持久化。

### 优化

- 重新授权与新增账户登录保持一致：生成登录链接后由用户选择“复制”或“打开”，不再自动打开浏览器，并补充复制失败提示和明确的邮件识别结果。
- 主窗口左上角的 Codex 运行状态在没有进程时显示为灰色，检测到运行进程后显示为绿色；“打开 Codex”按钮调整为蓝色。
- 已停用账户仍会显示明确的封号诊断、邮件来源和停用日期，便于后续核对或更新停用信息。

## [0.107.0] - 2026-07-29

### 新增

- 新增账户健康诊断，根据用量、账户检查、令牌刷新和登录授权的明确响应，区分授权失效、账号停用、工作区停用、请求受限和临时异常，并保留脱敏后的错误码、原始提示、确认时间和最近诊断记录。
- 授权失效的 ChatGPT 账户支持原位重新授权；成功后更新原账户凭据并校验登录身份，不会创建重复账户，非活跃账户也不会影响当前活跃账户。
- 授权页面未返回 Codex Switcher 时，支持一键确认“账号已删除或停用”，或粘贴完整错误内容进行严格识别；确认后立即结束等待并记录为用户从授权页面确认的诊断来源。

### 优化

- 在账户列表中直接提供“重新登录授权”“重新检测状态”和“查看诊断记录”入口，统一授权失效与手动停用状态的列对齐。
- 授权失效或明确停用的账户不再参与自动切换、批量用量轮询和自动预热；手动重新检测仍可使用现有凭据确认账户是否恢复。
- 删除账户改用二次弹窗确认，并明确展示删除成功、失败和失败原因。

### 修复

- 修复 ChatGPT 凭据失效后重新登录无法更新 Codex Switcher 中已有账户、重复导入又提示账户已存在的问题。
- 修复 OpenAI 在 `account_deactivated` 等身份验证错误页面不执行 OAuth 回调时，Codex Switcher 只能持续等待且无法记录真实停用信号的问题。

## [0.106.5] - 2026-07-29

### 新增

- 重置额度徽标支持展开查看每一项可用额度的名称、状态和本地到期时间，并按最近到期时间排序。

### 优化

- 优化 Windows 任务栏账户名称显示，保留“账号:”前缀；过长名称会智能从中间省略，悬停可查看完整名称，并增加布局安全间距避免内容重叠。

### 修复

- ChatGPT 用量、账户检查和预热请求改用浏览器风格请求头，降低被 Cloudflare 拦截为 403 的概率。

## [0.106.4] - 2026-07-26

### 修复

- 修复 Windows 应用内更新可能回退到 MSI、重新创建已删除桌面快捷方式的问题，并统一使用 NSIS 更新安装器。
- 修复主窗口从任务栏恢复后，鼠标未悬停在按钮上却显示“最小化”提示的问题。

## [0.106.3] - 2026-07-26

### 优化

- Free 账户的额度窗口根据接口返回的 30 天周期显示为“每月限制”，Plus 账户继续显示“每周限制”。
- 统一主页面、悬浮窗口、Windows 任务栏和托盘中的额度周期显示；Free 没有周额度时不再显示误导性的周额度。
- 到期时间和重置配额在红色临近状态下显示到分钟，其他状态继续保持原有日期显示。
- 升级 GitHub Actions 的 checkout、setup-node 和 pnpm 版本，并将 workflow 中执行项目脚本和构建的 Node 版本统一迁移到 24。

### 修复

- 修复订阅已到期的账户仍显示 Plus，以及 Windows 任务栏继续保留旧额度的问题。

## [0.106.2] - 2026-07-26

### 修复

- 修复应用启动时异步加载语言偏好后，语言选择器可能仍显示“跟随系统”的问题。

## [0.106.1] - 2026-07-26

### 新增

- 设置中新增可选的双时区时钟，在标题栏显示本地时间和 OpenAI/UTC 时间、日期与星期几。

### 优化

- 将双时区时钟居中放置在标题栏，并将时间精度调整为分钟，窄窗口下自动收起。

### 修复

- 修复 Windows 安装或静默更新时，在桌面自动恢复已被删除的快捷方式。

## [0.106.0] - 2026-07-23

### 新增

- 更新提示支持展示本次版本重点，并可跳转查看完整更新记录。
- 更新说明支持中英文，英文环境会显示英文重点并打开英文完整日志。

### 优化

- 统一应用与任务栏托盘图标，左键点击托盘图标直接打开主窗口。
- 重新整理托盘右键菜单，集中展示当前账户、快捷操作和显示组件。
- 使用原生 Windows 进程检测，并只跟踪实际的 Codex 桌面进程，减少切换前检查阻塞。
- 完善发布工具，严格校验版本、双语 Changelog、签名和跨平台更新清单。

### 修复

- 修复 Windows 进程检测堆积导致账户切换卡死的问题。
- 修复取消账户切换后托盘菜单出现多个对勾的问题。
- 修复托盘连续发起账户切换时状态错乱或重复处理的问题。
- 修复进程检查失败后界面继续显示旧状态，以及单账户刷新失败没有明确提示的问题。

## [0.105.0] - 2026-07-23

### 新增

- 主页面支持列表和卡片两种账户布局。
- 支持记忆主窗口尺寸、位置和最大化状态。

### 优化

- 调整标题栏、账户操作区和布局切换按钮的排列。
- 卡片布局直接融合账户详情信息，减少额外跳转。
- 优化账户筛选、订阅到期和 API 账户相关文案。
